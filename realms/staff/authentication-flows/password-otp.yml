alias: Username und Passwort und OTP oder Recovery
steps:
  - auth: auth-cookie
    requirement: ALTERNATIVE
  - auth: identity-provider-redirector
    requirement: ALTERNATIVE
  - alias: Username und Passwort
    requirement: ALTERNATIVE
    steps:
      - auth: auth-username-password-form
        requirement: REQUIRED
      - alias: OTP oder Recovery
        requirement: REQUIRED
        steps:
          - alias: OTP
            requirement: CONDITIONAL
            steps:
              - auth: conditional-user-configured
                requirement: REQUIRED
              - auth: auth-otp-form
                requirement: REQUIRED
          - alias: Recovery
            requirement: CONDITIONAL
            steps:
              - auth: conditional-user-configured
                requirement: REQUIRED
              - auth: auth-recovery-authn-code-form
                requirement: REQUIRED
